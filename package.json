{
	"name": "totallyhot-spark",
	"publisher": "davidpizon",
	"displayName": "TotallyHot Spark",
	"description": "The connective adapter that bridges GitHub Copilot Chat with an Agentic Router.",
	"icon": "assets/logo.png",
	"keywords": [
		"ai",
		"chat",
		"copilot",
		"github-copilot",
		"language-model",
		"llm",
		"openai",
		"ollama",
		"anthropic",
		"claude",
		"gemini",
		"openai-compatible",
		"openai-responses",
		"git-commit",
		"thinking"
	],
	"repository": {
		"type": "git",
		"url": "https://github.com/davidpizon/oai-compatible-copilot"
	},
	"version": "0.4.2",
	"engines": {
		"vscode": "^1.120.0"
	},
	"categories": [
		"AI",
		"Chat"
	],
	"badges": [
		{
			"url": "https://img.shields.io/github/stars/davidpizon/oai-compatible-copilot?style=social",
			"description": "Star oai-compatible-copilot on Github",
			"href": "https://github.com/davidpizon/oai-compatible-copilot"
		}
	],
	"bugs": {
		"url": "https://github.com/davidpizon/oai-compatible-copilot/issues"
	},
	"license": "MIT",
	"enabledApiProposals": [
		"chatProvider"
	],
	"contributes": {
		"languageModelChatProviders": [
			{
				"vendor": "totallyhot.spark",
				"displayName": "TotallyHot Spark"
			}
		],
		"commands": [
			{
				"command": "totallyhot.spark.openConfig",
				"category": "TotallyHot Spark",
				"title": "Open Configuration UI"
			},
			{
				"command": "totallyhot.spark.generateGitCommitMessage",
				"title": "Generate Commit Message with TotallyHot Spark",
				"category": "TotallyHot Spark",
				"icon": "$(wand)"
			},
			{
				"command": "totallyhot.spark.abortGitCommitMessage",
				"title": "Generate Commit Message with TotallyHot Spark - Stop",
				"category": "TotallyHot Spark",
				"icon": "$(debug-stop)"
			}
		],
		"menus": {
			"scm/title": [
				{
					"command": "totallyhot.spark.generateGitCommitMessage",
					"group": "navigation",
					"when": "config.git.enabled && scmProvider == git && !totallyhot.spark.isGeneratingCommit"
				},
				{
					"command": "totallyhot.spark.abortGitCommitMessage",
					"group": "navigation",
					"when": "config.git.enabled && scmProvider == git && totallyhot.spark.isGeneratingCommit"
				}
			],
			"commandPalette": [
				{
					"command": "totallyhot.spark.generateGitCommitMessage",
					"when": "config.git.enabled && !totallyhot.spark.isGeneratingCommit"
				},
				{
					"command": "totallyhot.spark.abortGitCommitMessage",
					"when": "config.git.enabled && totallyhot.spark.isGeneratingCommit"
				}
			]
		},
		"configuration": {
			"title": "TotallyHot Spark",
			"properties": {
				"totallyhot.spark.logLevel": {
					"type": "string",
					"default": "off",
					"enum": [
						"off",
						"debug",
						"info",
						"warn",
						"error"
					],
					"description": "Debug log level. Logs are written to ~/.copilot/totallyhot.spark/logs/ as JSON lines. Default is 'off' (no logging)."
				},
				"totallyhot.spark.baseUrl": {
					"type": "string",
					"default": "http://127.0.0.1:5001/v1",
					"description": "Base URL of the Agentic Router (or any OpenAI-compatible endpoint) this extension talks to. All chat requests for the single exposed model are sent here."
				},
				"totallyhot.spark.modelId": {
					"type": "string",
					"default": "agentic-router",
					"description": "The single model id sent (as the 'model' field) to the endpoint and shown in the Copilot model picker. Defaults to 'agentic-router' for single-model Agentic Router setups; set it to a configured route name (e.g. 'gpt-5.4') when the router does normal multi-model routing. Leave empty to expose no model. Not editable from the configuration UI."
				},
				"totallyhot.spark.modelName": {
					"type": "string",
					"default": "",
					"description": "(Optional) Display name for the model in the Copilot model picker. Defaults to the model id when empty."
				},
				"totallyhot.spark.apiMode": {
					"type": "string",
					"enum": [
						"openai",
						"openai-responses",
						"ollama",
						"anthropic",
						"gemini"
					],
					"default": "openai",
					"description": "Protocol used to talk to the endpoint: 'openai' (Default) for OpenAI Chat Completions (/chat/completions), 'openai-responses' for OpenAI Responses (/responses), 'ollama' for Ollama (/api/chat), 'anthropic' for Anthropic (/v1/messages), 'gemini' for Gemini (*/v1beta/models/{model}:streamGenerateContent?alt=sse)."
				},
				"totallyhot.spark.allowInsecureTls": {
					"type": "boolean",
					"default": false,
					"description": "Skip TLS certificate verification — only for localhost/127.0.0.1/::1 endpoints (e.g. a local dev proxy with a self-signed certificate). Never applied to remote hosts even when enabled. Leave disabled unless you understand the risk."
				},
				"totallyhot.spark.warnOnHttp1": {
					"type": "boolean",
					"default": true,
					"description": "Show a warning (and log it) the first time a request connects over HTTP/1.1 instead of negotiating HTTP/2 with the configured endpoint. Requests still work either way — HTTP/2 is opportunistic, not required."
				},
				"totallyhot.spark.retry": {
					"type": "object",
					"default": {
						"enabled": true,
						"max_attempts": 3,
						"interval_ms": 1000,
						"status_codes": []
					},
					"properties": {
						"enabled": {
							"type": "boolean",
							"default": true,
							"description": "Enable retry mechanism for api errors. Default is true."
						},
						"max_attempts": {
							"type": "number",
							"default": 3,
							"minimum": 1,
							"description": "Maximum number of retry attempts. Default is 3."
						},
						"interval_ms": {
							"type": "number",
							"default": 1000,
							"minimum": 1,
							"description": "Interval between retry attempts in milliseconds. Default is 1000 (1 seconds)."
						},
						"status_codes": {
							"type": "array",
							"items": {
								"type": "number"
							},
							"description": "Additional HTTP status codes that will be merged. Default is [429, 500, 502, 503, 504]."
						}
					},
					"description": "Retry configuration for handling api errors like [429, 500, 502, 503, 504]."
				},
				"totallyhot.spark.delay": {
					"type": "number",
					"default": 0,
					"minimum": 0,
					"description": "Fixed delay in milliseconds between consecutive requests. Default is 0 (no delay)."
				},
				"totallyhot.spark.commitLanguage": {
					"type": "string",
					"default": "English",
					"enum": [
						"English",
						"French",
						"Italian",
						"German",
						"Spanish",
						"Russian",
						"Chinese (Simplified)",
						"Chinese (Traditional)",
						"Japanese",
						"Korean",
						"Czech",
						"Portuguese (Brazil)",
						"Turkish",
						"Polish"
					],
					"description": "Language for generated Git commit messages. Default is English."
				},
				"totallyhot.spark.commitMessagePrompt": {
					"type": "string",
					"default": "",
					"description": "Custom system prompt for commit message generation. If not provided, uses the default prompt."
				},
				"totallyhot.spark.readFileLines": {
					"type": "number",
					"default": 0,
					"minimum": 0,
					"maximum": 10000,
					"description": "Number of lines to read when using `read_file` tool. Large lines may cost more tokens. Default is 0, let model decide lines."
				}
			}
		}
	},
	"main": "./out/extension.js",
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"download-api": "dts dev && mv vscode.proposed.*.ts src",
		"compile": "tsc -p ./",
		"lint": "eslint",
		"format": "prettier --write .",
		"watch": "tsc -watch -p ./",
		"test": "npm run compile && vscode-test",
		"build": "npx @vscode/vsce package -o extension.vsix"
	},
	"dependencies": {
		"@microsoft/tiktokenizer": "^1.0.10",
		"undici": "^8.9.0"
	},
	"devDependencies": {
		"@eslint/js": "^10.0.1",
		"@stylistic/eslint-plugin": "^5.10.0",
		"@types/node": "^26",
		"@types/mocha": "^10.0.6",
		"@vscode/dts": "^0.4.1",
		"@types/vscode": "^1.125.0",
		"@vscode/test-cli": "^0.0.15",
		"@vscode/test-electron": "^3.1.0",
		"eslint": "^10.8.0",
		"prettier": "^3.9.6",
		"typescript": "^7.0.2",
		"typescript-eslint": "^8.64.0"
	},
	"overrides": {
		"serialize-javascript": "^7.0.5"
	}
}

